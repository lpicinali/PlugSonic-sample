export const sourceSelector = state => state.source

export const sourceLoadingSelector = state => state.source.loading

export const sourceNameSelector = state => state.source.name

export const sourceMediaSelector = state => state.source.media

export const sourceErrorSelector = state => state.source.error

export const sourceActionSelector = state => state.source.action

export const seekSelector = state => state.source.seek

export const regionSelector = state => state.source.region
